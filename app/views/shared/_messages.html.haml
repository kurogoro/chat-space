.messages
  - @messages.each do |message|
    .message
      .upper-info
        %p.upper-info__user
          = message.user.name
        %p.upper-info__date
          = message.created_at.strftime("%Y/%m/%d(%a) %H:%M:%S")
      %p.message__text
        = message.content