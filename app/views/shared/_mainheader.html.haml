.main-header
  .current-group
    %h2.current-group__name
      = @group.name
    %ul.member-list
      Member :&nbsp;
      - @group.group_users.each do |group_user|
        %li.member-list__member
          = group_user.user.name
  = link_to edit_group_path(@group.id), style: "text-decoration:none;" do
    .main-header__edit-btn
      Edit